/**
 * The browser's import point for the API's shapes. They live in
 * `shared/types/api.ts` so the server can annotate its routes with the same
 * declarations; this file keeps `~/types` working for every app import.
 */
export * from '#shared/types/api'
